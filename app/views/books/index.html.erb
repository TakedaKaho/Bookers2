<h1>Books</h1>

<div class ="index_head">
    <th>Title</th>
    <th>Opinion</th>
</div>

<% @books.each do |book| %>
<%= image_tag book.user.get_profile_image(100, 100) %>
 <%= book.title %>
 <%= book.body %>
 <% end %>